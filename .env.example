# Postgres connection string.
# Injected automatically into Vercel deployments by the Neon integration.
# For local development, run `vercel env pull .env.local` to fetch it.
DATABASE_URL=

# Secret used to sign the session cookie. Any long random string.
# Generate one with: npm run --silent gen-secret
# Changing this signs every device out.
AUTH_SECRET=

# Your login passcode, in plain text. Pick whatever you like.
# Stored unhashed on purpose - see src/lib/passcode.ts for the reasoning.
APP_PASSCODE=
